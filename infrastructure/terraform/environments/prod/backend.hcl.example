# =============================================================================
# Terraform Backend Configuration - Prod Environment
# =============================================================================
# This file is generated by running terraform apply in terraform/backend/
# Copy this example to backend.hcl and update with your values.
#
# Usage: terraform init -backend-config=backend.hcl
# =============================================================================

bucket         = "quikapp-terraform-state-ACCOUNT_ID"
key            = "prod/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
dynamodb_table = "quikapp-terraform-locks"

# Optional: KMS encryption (if enabled in backend module)
# kms_key_id   = "arn:aws:kms:us-east-1:ACCOUNT_ID:key/KEY_ID"
