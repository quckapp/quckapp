name: "CI/CD — moderation-service"

on:
  push:
    branches: [main]
    paths: ['services/moderation-service/**']
  pull_request:
    paths: ['services/moderation-service/**']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps and test
        working-directory: services/moderation-service
        run: |
          pip install -r requirements.txt
          pytest -v

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-service-deploy.yml
    with:
      service_name: moderation-service
      service_path: services/moderation-service
      language: python
      port: '5014'
    secrets: inherit
