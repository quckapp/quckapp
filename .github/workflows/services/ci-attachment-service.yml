name: "CI/CD — attachment-service"

on:
  push:
    branches: [main]
    paths: ['services/attachment-service/**']
  pull_request:
    paths: ['services/attachment-service/**']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Run tests
        working-directory: services/attachment-service
        run: go test -v ./...

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-service-deploy.yml
    with:
      service_name: attachment-service
      service_path: services/attachment-service
      language: go
      port: '4011'
    secrets: inherit
