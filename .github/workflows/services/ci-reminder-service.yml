name: "CI/CD — reminder-service"

on:
  push:
    branches: [main]
    paths: ['services/reminder-service/**']
  pull_request:
    paths: ['services/reminder-service/**']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Run tests
        working-directory: services/reminder-service
        run: go test -v ./...

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-service-deploy.yml
    with:
      service_name: reminder-service
      service_path: services/reminder-service
      language: go
      port: '4010'
    secrets: inherit
