name: "CI/CD — thread-service"

on:
  push:
    branches: [main]
    paths: ['services/thread-service/**']
  pull_request:
    paths: ['services/thread-service/**']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Run tests
        working-directory: services/thread-service
        run: go test -v ./...

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-service-deploy.yml
    with:
      service_name: thread-service
      service_path: services/thread-service
      language: go
      port: '3005'
    secrets: inherit
