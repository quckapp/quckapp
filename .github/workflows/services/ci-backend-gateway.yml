name: "CI/CD — backend-gateway"

on:
  push:
    branches: [main]
    paths: ['services/backend-gateway/**']
  pull_request:
    paths: ['services/backend-gateway/**']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install deps and test
        working-directory: services/backend-gateway
        run: |
          npm ci
          npm test

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-service-deploy.yml
    with:
      service_name: backend-gateway
      service_path: services/backend-gateway
      language: nestjs
      port: '3000'
    secrets: inherit
