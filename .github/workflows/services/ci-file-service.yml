name: "CI/CD — file-service"

on:
  push:
    branches: [main]
    paths: ['services/file-service/**']
  pull_request:
    paths: ['services/file-service/**']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Run tests
        working-directory: services/file-service
        run: go test -v ./...

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-service-deploy.yml
    with:
      service_name: file-service
      service_path: services/file-service
      language: go
      port: '5002'
    secrets: inherit
