name: "CI/CD — integration-service"

on:
  push:
    branches: [main]
    paths: ['services/integration-service/**']
  pull_request:
    paths: ['services/integration-service/**']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps and test
        working-directory: services/integration-service
        run: |
          pip install -r requirements.txt
          pytest -v

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-service-deploy.yml
    with:
      service_name: integration-service
      service_path: services/integration-service
      language: python
      port: '5016'
    secrets: inherit
