name: "CI/CD — channel-service"

on:
  push:
    branches: [main]
    paths: ['services/channel-service/**']
  pull_request:
    paths: ['services/channel-service/**']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Run tests
        working-directory: services/channel-service
        run: go test -v ./...

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-service-deploy.yml
    with:
      service_name: channel-service
      service_path: services/channel-service
      language: go
      port: '5005'
    secrets: inherit
