name: "CI/CD — huddle-service"

on:
  push:
    branches: [main]
    paths: ['services/huddle-service/**']
  pull_request:
    paths: ['services/huddle-service/**']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: '1.15'
          otp-version: '26'
      - name: Install deps and test
        working-directory: services/huddle-service
        run: |
          mix deps.get
          mix test

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-service-deploy.yml
    with:
      service_name: huddle-service
      service_path: services/huddle-service
      language: elixir
      port: '4005'
    secrets: inherit
