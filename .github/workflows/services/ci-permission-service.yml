name: "CI/CD — permission-service"

on:
  push:
    branches: [main]
    paths: ['services/permission-service/**']
  pull_request:
    paths: ['services/permission-service/**']
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run tests
        working-directory: services/permission-service
        run: mvn test -B

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/reusable-service-deploy.yml
    with:
      service_name: permission-service
      service_path: services/permission-service
      language: spring
      port: '8083'
    secrets: inherit
