# Test Environment Configuration
# This file is used for running tests with isolated settings

NODE_ENV=test

# JWT Configuration for tests
JWT_SECRET=test-jwt-secret-key-for-testing-purposes-only
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=test-refresh-secret-key-for-testing-purposes
JWT_REFRESH_EXPIRES_IN=7d

# Encryption key for tests (must be 32 characters)
ENCRYPTION_KEY=test-encryption-key-32-chars!!

# MongoDB - Will be overridden by mongodb-memory-server
MONGODB_URI=mongodb://localhost:27017/quickchat-test

# Disable external services in tests
USE_REDIS_IN_DEV=false
REDIS_HOST=
REDIS_PORT=

# Firebase - Disabled in tests
FIREBASE_PROJECT_ID_DEV=
FIREBASE_PRIVATE_KEY_DEV=
FIREBASE_CLIENT_EMAIL_DEV=

# SMTP - Disabled in tests
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# External APIs - Disabled in tests
OPENAI_API_KEY=
SENTRY_DSN=

# Rate limiting - More permissive for tests
THROTTLE_TTL=60
THROTTLE_LIMIT=1000
