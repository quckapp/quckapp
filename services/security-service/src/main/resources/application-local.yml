spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quckapp_security?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: rootpassword
  jpa:
    show-sql: true
  kafka:
    bootstrap-servers: localhost:29092

security-service:
  waf:
    mode: DETECT
  geoip:
    enabled: false

logging:
  level:
    com.quckapp.security: DEBUG
    org.hibernate.SQL: DEBUG
