{
  "name": "@quckapp/service-urls",
  "version": "1.0.0",
  "description": "Centralized API service URL configuration with semantic versioning and per-environment version management",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "quckapp",
    "service-urls",
    "api-versioning"
  ],
  "license": "UNLICENSED",
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
